generator: admingenerator.generator.doctrine
params:
    model: Nitra\DeliveryBundle\Entity\Delivery
    namespace_prefix: Nitra
    concurrency_lock: ~
    bundle_name: DeliveryBundle
    pk_requirement: ~
    fields:
        name: { label: "Название" }
    object_actions:
        delete: ~
    batch_actions:
        delete: ~
builders:
    list:
        params:
            title:          "Список служб доставки"
            display:        [ name ]
            max_per_page:   100
            actions:
                new:    ~
            object_actions:
                edit:   ~
                delete: ~
    filters:
        params:
            display:    [ name ]
    new:
        params:
            title:      "Добавить новую службу доставки"
            display:    [ name ]
            sort:       [ name, ASC ]
            actions:
                save: ~
                save_and_List: ~
                list: ~
    edit:
        params:
            title:      "Редактировать службу доставки \"%object%\"|{ %object%: Delivery.name }|"
            display:    [ name ]
            actions:
                save: ~
                save_and_List: ~
                list: ~
    show:
        params:
            title:      "Просмотр службы доставки \"%object%\"|{ %object%: Delivery.name }|"
            display:    ~
            actions:
                list: ~
                new: ~
    actions:
        params:
            object_actions:
                delete: ~
            batch_actions:
                delete: ~
