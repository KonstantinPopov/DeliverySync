generator: admingenerator.generator.doctrine
params:
  model: Nitra\DeliveryBundle\Entity\Department
  namespace_prefix: Nitra
  bundle_name: DeliveryBundle
  fields: 
    name:
      label:  Название
    address:
      label:  Адрес
    phone:
      label:  Телефон
    latitude:
      label:  Широта
    longitude:
      label:  Долгота
    deliveryService:
      label:  Транспортная компания
    deliveryCity:
      label:  Город

builders:
  list:
    params:
      title: Список отделений
      display: [ name, address, phone, latitude, longitude, deliveryService, deliveryCity  ]
      max_per_page:  100
      actions:
        new: {}
      object_actions:
        edit: ~
        delete: {}
  filters:
    params:
      display: [ name, address, phone, deliveryService, deliveryCity ]
  new:
    params:
      title: Новое отделение
      display: [ name, address, phone, latitude, longitude ]
      actions:
        list: ~
  edit:
    params:
      title: редактирование отделения "%object%"|{ %object%: Delivery.name }|
      display: [ name, address, phone, latitude, longitude ]
      actions:
        list: ~
  delete: ~
